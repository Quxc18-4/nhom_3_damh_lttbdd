name: nhom_3_damh_lttbdd
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_custom_clippers: ^2.1.0
  font_awesome_flutter: ^10.10.0
  convex_bottom_bar: ^3.2.0
  intl: ^0.20.2

  # === FIREBASE PACKAGES ===
  firebase_core: ^4.2.0
  firebase_database: ^12.0.3
  cloud_firestore: ^6.0.3
  firebase_auth: ^6.1.1
  firebase_storage: ^13.0.3
  flutter_facebook_auth: ^6.1.1
  google_sign_in: ^6.2.0

  # === UI / UTILITIES ===
  google_fonts: ^6.3.2
  shared_preferences: ^2.5.3
  image_picker: ^1.2.0
  flutter_svg: ^2.2.1
  flutter_expandable_fab: ^2.5.2
  http: ^1.5.0
  cloudinary_url_gen: ^1.8.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  flutter_localization: ^0.3.3
  flutter_rating_bar: ^4.0.1
  mockito: ^5.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  integration_test:
    sdk: flutter

  # fake Firestore cho test (TƯƠNG THÍCH Firestore 6.x)
  fake_cloud_firestore: ^4.0.1

  flutter_lints: ^5.0.0
  build_runner: ^2.10.4
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/maps/
